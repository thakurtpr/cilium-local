# Test Credentials for WASM Filter Authentication
#
# These are sample credentials for testing the oxy_money_auth_filter
# Replace with your actual credentials before running tests

# Pass-key header value
# This is the API key or pass-key expected by the WASM filter
PASS_KEY="test-pass-key-12345"

# Valid JWT token
# This should be a valid JWT that your filter accepts
# The example below is a test JWT with:
#   - Header: {"alg":"HS256","typ":"JWT"}
#   - Payload: {"sub":"1234567890","name":"Test User","iat":1516239022,"exp":9999999999}
#   - Signature: signed with secret "test-secret"
VALID_JWT="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IlRlc3QgVXNlciIsImlhdCI6MTUxNjIzOTAyMiwiZXhwIjo5OTk5OTk5OTk5fQ.4Adcj0PYg1dJ8RXB1xKLrgbWxPHP7w9C8q5HN_8u4u8"

# Invalid JWT token (for negative testing)
INVALID_JWT="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJpbnZhbGlkIn0.invalid"

# Alternative pass-key for testing
ALT_PASS_KEY="alternative-pass-key"

# Backend service configuration
SERVICE_NAME="test-backend"
NAMESPACE="cilium-test"
SERVICE_PORT="8080"

# ============================================================
# How to customize:
# ============================================================
#
# 1. Update PASS_KEY with your actual API key/pass-key
#    This is the header value your WASM filter expects in the 'pass-key' header
#
# 2. Update VALID_JWT with a valid JWT token
#    Generate a JWT using your signing secret:
#    - Go to https://jwt.io
#    - Or use: echo -n '{"sub":"user123"}' | base64
#
# 3. Test the credentials:
#    source test-credentials.env
#    ./test-auth.sh
#
# ============================================================
# Generating a test JWT:
# ============================================================
#
# Using jwt-cli (https://github.com/mike-engel/jwt-cli):
#   jwt encode --secret test-secret '{"sub":"user123","name":"Test User"}'
#
# Using Python:
#   python3 -c "import jwt; print(jwt.encode({'sub':'user123','name':'Test'}, 'secret', algorithm='HS256'))"
#
# Using Node.js:
#   node -e "const jwt = require('jsonwebtoken'); console.log(jwt.sign({sub:'user123'}, 'secret'))"
#
# ============================================================
